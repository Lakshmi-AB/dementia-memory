# Supabase environment variables
# These are pre-populated in .env — do not modify unless reconnecting to a different project.
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Future: AI trivia generation API key (for connecting an LLM to generate dynamic trivia)
# VITE_OPENAI_API_KEY=
